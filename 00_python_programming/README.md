# 完整 Python 程序

本模块训练从空文件开始组织可直接运行的程序，而不是只补一个函数。

## 当前专题

| 专题 | 训练目标 | 状态 |
| --- | --- | --- |
| `01_standard_input/` | `main()`、标准输入、形状检查、标准输出、错误退出 | 已建参考闭环，等待学生练习 |

推荐顺序：先读专题说明，独立完成 `starter.py`，再运行样例；遇到问题时先定位错误，最后才对照 `reference/solution.py`。
