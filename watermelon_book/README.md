# 周志华《机器学习》逐章主线

本目录将在基础训练完成后逐章增加真实内容，不提前创建16章空文件。当前权威状态见根目录 `coverage_matrix.md`，全部章节仍为“未开始”。

优先建设顺序：模型评估 → 线性模型 → KNN/PCA/K-means → 决策树 → 贝叶斯分类 → 神经网络 → 集成学习 → SVM → 其余章节。

正式细化覆盖矩阵前，需要核对学习者实际使用版本的教材目录。
