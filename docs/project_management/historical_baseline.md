# 历史学习材料基线

## 来源与使用边界

2026-07-17 对 `D:\python_work` 做过只读检查。该目录是历史学习记录，不属于当前仓库，也不应成为新仓库运行所依赖的外部路径。

历史文件只能说明“过去接触或写过”，不能证明“现在仍会”。当前自评是 NumPy 和 pandas 基础已经遗忘较多，因此所有能力都需要通过预测、独立编码、测试和变式题重新验证。

## 过去接触过的内容

### Python

- 基础语法、列表、字典、循环、函数和类；
- 文本与 JSON 文件读写；
- 异常处理；
- 数据可视化练习。

### NumPy

- 数组创建、`shape`、`ndim`、`dtype` 和转置；
- 索引、切片、布尔筛选；
- 随机数与随机种子；
- 逐元素运算、广播和矩阵乘法；
- 聚合、排序、去重、拼接、分割和 reshape。

### pandas

- Series 和 DataFrame；
- `loc`、`iloc`、布尔筛选；
- 缺失值、描述统计、排序和去重；
- 时间索引、重采样、变化率和简单数据分析。

### 机器学习

- 线性回归的梯度下降实现；
- Logistic 回归的 sigmoid、梯度和概率预测；
- 决策树的熵、信息增益、最佳特征和递归草稿。

## 历史材料暴露出的重点薄弱点

1. `(n,)` 与 `(n, 1)` 的区别和 reshape 的目的；
2. `axis=0`、`axis=1` 与删行、删列、聚合方向；
3. 布尔掩码按样本位置同步筛选 `X` 和 `y`；
4. `X @ w`、`X.T @ error` 的形状推理；
5. 学习率、梯度尺度、shape 错误导致训练发散时的排查顺序；
6. 决策树递归、子集切分、已用特征删除和特征名同步；
7. 代码能够运行后，对边界、数值和细节的二次审查。

## 对当前课程设计的影响

- 从基础重新讲，但不是机械重复旧笔记；
- 优先安排 shape、axis、索引、广播和矩阵乘法诊断；
- 每个旧知识点先用小题测试，证明掌握后才加速；
- pandas 放在 NumPy 基础和文件读写之后复习；
- 线性回归、Logistic 回归和决策树不直接沿用旧答案，而是从空白重建并加入变式与完整文件任务；
- 历史决策树错误将来可转成纠错题，但不会提前写入当前 `records/bug_book/` 作为现阶段错误。

## 当前能力判定原则

历史记录与当前自评冲突时，以当前自评和现场表现为准。只有满足以下证据才能更新当前状态：

1. 运行前能预测形状或主要输出；
2. 不看旧文件和参考答案完成实现；
3. 通过正常、边界和性质测试；
4. 完成至少一道改变条件的迁移题；
5. 能用自己的话解释关键代码。
