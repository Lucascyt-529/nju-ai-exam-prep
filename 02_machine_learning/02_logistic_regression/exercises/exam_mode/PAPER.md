# 二分类逻辑回归完整程序（建议 60 分钟）

读取三份 CSV。训练/验证列为 `id,x1,x2,label`，测试列为 `id,x1,x2`。只用训练数据确定标准化参数和模型参数；在验证集报告 accuracy、precision、recall、F1 与 AUC；保存并重新加载模型后输出测试概率和0/1标签。

`predictions.csv` 表头为 `id,probability,label`，概率保留6位小数。终端逐行打印五个验证指标。程序必须能从命令行直接运行。

评分：读取与训练统计量20分，训练25分，分类评估20分，模型恢复15分，预测文件10分，完整程序10分。
