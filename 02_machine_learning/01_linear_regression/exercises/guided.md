# 引导式练习

使用 `X=[[0],[1],[2]]`、`y=[1,3,5]`：

1. 先实现 `predict`，核对预测值；
2. 实现 MSE，核对零参数下结果为 `35/3`；
3. 实现梯度，核对 `gradient_w=[-26/3]`、`gradient_b=-6`；
4. 手动做一次参数更新，再写入循环；
5. 让损失历史先保存初始损失，因此长度应为 `n_steps+1`；
6. 最后用常数列和 `np.linalg.lstsq` 实现最小二乘。

每一步都运行 `check.py`。预期手算结果可回到专题 [README](../README.md) 核对。
