# kNN 分类完整程序（建议 45 分钟）

读取训练、验证、测试 CSV；训练/验证列为 `id,x1,x2,label`，测试无 label。特征标准化参数只由训练集确定。用验证集在候选 `k=1,3` 中选择准确率更高者，平局选较小 k；保存必要模型数据，重新加载后预测测试集。

输出 `predictions.csv`，表头 `id,label`；终端打印所选 k 与验证准确率。程序须可直接运行。

评分：读取和预处理20分，距离/邻居/平局规则30分，验证选择15分，保存恢复15分，输出与入口20分。
