# PCA 降维完整程序（建议 50 分钟）

读取训练和测试 CSV，列为 `id,x1,x2,x3`。只用训练集估计中心，选择能让累计解释方差比例不低于0.90的最少主成分；报告训练重构 MSE。保存并重新加载模型后变换测试集。

输出 `embedding.csv`，第一列为 id，其余列依次命名 `pc1,pc2,...`，数值保留6位小数；终端打印维数、解释方差比例和重构 MSE。

评分：读取15分，中心化与协方差20分，谱分解和排序25分，评价15分，保存恢复与输出15分，入口10分。
