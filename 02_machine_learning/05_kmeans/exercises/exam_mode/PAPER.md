# K-means 聚类完整程序（建议 60 分钟）

读取训练和测试 CSV，列为 `id,x1,x2`。标准化参数只从训练集估计。分别用 `k=2,3`、固定随机种子训练，在训练集上选择 SSE 更小的方案；保存并重新加载模型后为测试样本分配簇。

输出 `clusters.csv`，表头 `id,cluster`；终端打印所选 k、SSE 和迭代次数。相同输入必须产生相同结果。

评分：读取和预处理20分，初始化/分配/更新30分，停止与空簇处理15分，方案评价10分，保存恢复与输出15分，入口10分。
