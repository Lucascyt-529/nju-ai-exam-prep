# 项目续接状态

这个文件是 Codex 在新会话、上下文压缩或跨设备后恢复工作的第一入口。它记录“仓库建设到哪里”，不替代 `progress.md` 中的个人学习记录，也不替代 `coverage_matrix.md` 中的教材覆盖状态。

## 最后更新

- 日期：2026-07-17
- 当前分支：`main`
- 远程仓库：`Lucascyt-529/nju-ai-exam-prep`（私有）
- 最近完成批次：Phase 12 第三十五批第8章Boosting指数风险视角

提交哈希不要手工复制到这里；恢复时使用 `git log -5 --oneline` 获取真实记录，避免状态文件与提交产生循环更新。

## 总目标

建设一套用于南京大学人工智能学院预推免机试的长期训练仓库，覆盖：

1. Python 完整程序与无 OJ 自检；
2. 文件读写、NumPy、pandas 与数据处理；
3. 以周志华《机器学习》为主线的完整知识覆盖与 NumPy 核心算法手写；
4. 常见算法题、综合任务和限时模拟；
5. 能迁移、能解释、能独立实现，而不是背参考答案。

## 已确认的长期决定

- 学习者的 NumPy 和 pandas 基础较薄弱，旧学习记录不能作为当前掌握证据。
- 采用“建设一批 → 实际学习/诊断 → 调整粒度 → 建设下一批”的循环。
- 学生版、参考实现和测试分离；未经请求不替学习者填写 `starter.py`。
- 机器学习核心实现默认只依赖 Python 标准库和 NumPy。
- pandas 单独教学，用于表格数据处理，不替代 NumPy 核心算法。
- 所有完整题目必须覆盖读取、检查、处理、算法、输出和自测。
- 未核对教材版次与实际目录前，不得声称周志华《机器学习》已经无遗漏覆盖。
- 用户已允许当前仓库的小批次改动在验证后直接提交并推送到 `main`；推送前仍须检查状态和测试。
- 当前阶段优先完整建设并逐项验证教材第1～10章；第11～16章保留长期范围但暂缓，待前十章完成后再启动。

## 当前建设状态

Phase 1 已完成并推送：

- 仓库规范、学习方法、课程清单、覆盖矩阵、进度和错题记录；
- Python 标准输入与 TXT/CSV 文件读写基础闭环；
- NumPy 形状、索引、广播、矩阵乘法基础专题；
- 缺失值填补、标准化与数据泄漏基础专题；
- 学生版、参考实现、样例数据与基础自动测试；
- pandas、教材章节、算法题、综合题和模拟考试的导航骨架。

Phase 2 第一批已完成并纳入本批提交：

- 新增 NumPy 数组创建、`ndim`、`shape`、`size` 与 `dtype` 的前置专题；
- 新增 `(n,)`/`(n, 1)` 转换、转置、常数列和样本拼接练习；
- 将 pandas 加入明确依赖，并以本机 pandas 2.2.3 完成验证；
- 新增 Series/DataFrame、列选择和转 NumPy 的第一个 pandas 闭环；
- 新增 CSV 读取、`.iloc`、`.loc` 和布尔筛选的第二个 pandas 闭环；
- 新增19项针对上述内容的参考实现测试，全量测试增至65项。

Phase 2 第二批已完成并纳入本批提交：

- 新增标准库 `csv` 混合类型读取专题，覆盖字符串编号/姓名、逐行校验、异常输入和严格输出；
- 新增 pandas 缺失值、非法数字、重复编号、类型转换、中位数拟合/复用和严格保存专题；
- 新增第一个完整无 OJ 综合任务：训练/测试 CSV、训练集拟合、参数 `.npz` 保存与恢复、测试变换和命令行输出；
- 专门验证单行二维形状、训练/测试隔离、整列缺失、重复编号、错误表头、损坏参数和逐字节结果；
- 新增21项测试，全量测试从65项增至86项并已通过。

Phase 3 第一批已完成并纳入本批提交：

- 正式建立 `watermelon_book/02_model_evaluation_selection/`，不再维持16章全为空目录；
- 新增留出、分层留出、K折、分层K折和自助采样索引专题；
- 新增MAE/MSE/RMSE/R²、二分类混淆度量、ROC/PR曲线、AUC和代价敏感错误；
- 新增通用折内训练、折外预测、折分数摘要和候选方案方向选择；
- 新增29项测试，覆盖可复现性、互斥覆盖、类别比例、零分母、常数目标、已知AUC、折内隔离和错误折定义；
- 覆盖矩阵只把有实现和测试的评估方法/度量/CV标为“已验证”，比较检验与偏差—方差保持未开始。

Phase 3 第二批当前检查点已完成并纳入本批提交：

- 建立第3章线性模型目录与统一形状约定；
- 实现线性预测、MSE、MSE解析梯度、`np.linalg.lstsq`最小二乘和批量梯度下降；
- 用有限差分验证梯度，用精确直线验证两种拟合方法，并覆盖秩亏设计矩阵；
- 明确拒绝 `(n,1)` 标签与 `(n,)` 预测直接混算，复用第一次形状诊断的真实薄弱点；
- 新增10项线性回归核心函数测试。

Phase 3 第二批完整闭环随后已补齐：

- 新增训练、验证、测试三份混合CSV的独立命令行任务；
- 只用训练集拟合，在验证集报告MSE/R²，模型保存后重新加载再预测测试集；
- 严格输出预测CSV和指标文本，并逐字节比较；
- 验证修改验证目标不会改变训练模型，覆盖单行测试、重复编号、错误表头和非有限值；
- 新增8项测试，线性回归核心函数与完整任务均有独立验证证据。

Phase 3 第三批已完成并纳入本批提交：

- 新增多项式特征、最小二乘拟合、复杂度曲线和学习曲线；
- 用固定噪声样例验证训练误差降至0时验证误差反而回升；
- 新增重复采样的偏差平方、方差、噪声与期望误差数值分解；
- 验证固定种子可复现，以及高次多项式在小样本下方差更高；
- 新增10项测试，并将第2章过拟合与偏差—方差条目更新为“已验证”。

Phase 3 第四批已完成并纳入本批提交：

- 新增精确二项下尾概率、配对t统计量和修正重采样t统计量；
- 新增McNemar不一致计数与双侧精确概率；
- 新增多数据集方向感知秩、并列平均秩、Friedman统计量；
- 新增Nemenyi临界差与显著差异对；
- 对标准库/NumPy没有可靠分布函数的检验显式返回统计量并接收表格临界值，不伪造近似p值；
- 新增14项测试，第2章主要编程条目均已有参考闭环，但仍待实际教材目录逐小节核验。

Phase 3 第五批已完成并纳入本批提交：

- 新增正负分支稳定sigmoid和直接从logits计算的稳定二元交叉熵；
- 新增概率预测、阈值分类、带L2损失与解析梯度；
- 新增批量梯度下降，统一 `X:(n,d)`、`y:(n,)`、`w:(d,)` 形状约定；
- 用有限差分验证带L2梯度，并覆盖极端logit、正则强度、决策阈值和输入不原地修改；
- 新增12项测试；完整CSV分类任务尚待下一批建设。

Phase 3 第六批已完成并纳入本批提交：

- 新增训练、验证、测试三份二分类CSV的完整命令行任务；
- 标准化均值与尺度只从训练集拟合，并与权重、截距、阈值共同保存到 `.npz`；
- 模型重新加载后才处理测试集，输出6位概率与0/1标签；
- 验证集报告accuracy、precision、recall、F1和使用连续概率计算的AUC；
- 验证修改验证数据不会改变模型包，覆盖单行测试、错误标签、重复编号、非有限值和单类别AUC边界；
- 新增11项测试，对数几率回归核心与完整任务均有独立证据。

Phase 3 第七批已完成并纳入本批提交：

- 新增二分类LDA的类别均值、类内散度矩阵、Fisher投影方向与中点阈值判别；
- 用 `np.linalg.pinv` 处理重复特征导致的奇异类内散度，并提供可选非负正则化；
- 新增投影、标签预测与Fisher比率，明确权重缩放不改变分类语义；
- 验证整体平移不改变预测、投影方向缩放时阈值同步缩放，以及类内方差为零时Fisher比率为无穷；
- 拒绝单类别、多于两类、类别均值相同、负正则和权重形状错误等无效输入；
- 新增11项测试，并运行引导演示核对类均值、散度、权重、阈值和投影数值。

Phase 3 第八批已完成并纳入本批提交：

- 新增一对其余（OvR）训练、`(n,K)`分数矩阵和最大分数预测；
- 新增一对一（OvO）全部 `K(K-1)/2` 类别对、两类子集训练、成对分数和投票矩阵；
- 使用透明的类中心线性打分器展示拆分控制流程，并明确它不替代对数几率回归或LDA；
- 支持 `10、20、30` 这类非连续数值标签，内部下标最终映射回原标签；
- 明确零分数投给较前类别、总票数相同选择升序类别表较前标签的确定性规则；
- 新增15项测试，覆盖三类/四类、分数与投票形状、组合数、平票、模型完整性和输入不原地修改。

Phase 3 第九批已完成并纳入本批提交：

- 新增升序类别计数、平衡类别权重与逐样本权重，验证每类权重总和相同且全体均值为1；
- 新增可复现的随机欠采样与过采样索引，支持二分类和多分类，并强调同一索引同步处理特征与标签；
- 欠采样不重复索引，过采样保留所有原样本并只复制少数类，采样只允许在训练集内部进行；
- 新增假正例/假负例代价阈值，验证更高假负例代价会降低阈值并增加正类决策；
- 新增先验漂移下的概率修正和等价原概率阈值，明确其假设强于单纯阈值移动；
- 新增17项测试，并用90/10样例展示高accuracy与零正类recall可以同时出现。

Phase 4 第一批已完成并纳入本批提交：

- 新增信息熵、基尼值、信息增益、增益率和加权基尼指数的可手算实现；
- 增益率采用“信息增益不低于候选平均值，再比较增益率”的两步规则，三种准则平局均选较小特征下标；
- 新增离散特征递归建树，节点保存多数类作为空分支和未见属性值的确定性回退；
- 覆盖纯节点、特征耗尽、当前子集剩余特征恒定和全局属性域中当前空分支等停止边界；
- 验证XOR根节点两个信息增益均为0时仍可继续递归并正确拟合，避免错误的“零增益立即停止”；
- 新增21项测试，并运行父熵与候选特征信息增益的引导演示。

Phase 4 第二批已完成并纳入本批提交：

- 新增验证集预剪枝：划分特征和分数只由训练子集确定，验证子集只决定是否保留当前划分；
- 只有一层划分验证准确率严格高于多数类叶节点时才继续生长，相同准确率采用较小叶节点；
- 没有验证样本到达当前节点时保守停止，避免在缺乏验证证据时继续增加复杂度；
- 新增自底向上的后剪枝，先处理子树，再比较当前子树与多数类叶节点的验证准确率；
- 后剪枝深复制输入树，支持可靠比较剪枝前后节点数且不破坏完整树；
- 新增13项剪枝测试，覆盖训练/验证职责、平局、无益/有益子树、未见值回退、非原地修改和输入边界。

Phase 4 第三批已完成并纳入本批提交：

- 新增连续特征相邻不同值中点候选，重复值之间不生成无效阈值；
- 新增信息增益与加权基尼两种连续二分评分，分数相同按较小特征下标和较小阈值稳定选择；
- 新增离散/连续混合树，离散特征沿路径移除，连续特征在子节点仍可再次用于形成多个区间；
- 连续节点对训练范围外有限数值仍按阈值进入左右分支，离散节点对未见取值回退当前多数类；
- 明确暂不把连续属性增益率、缺失值权重和多变量划分伪装成已完成内容；
- 新增17项测试，覆盖重复值、完美二分、平局、三段区间、混合节点、范围外预测和非原地修改。

Phase 4 第四批已完成并纳入本批提交：

- 新增带权熵与缺失属性信息增益，只用非缺失样本评价划分并乘非缺失权重比例；
- 新增透明的 `(n,V)` 分支权重计划：已知样本只进匹配分支，缺失样本按已知分支质量比例传播；
- 验证每个样本跨分支权重之和等于原权重，权重为0的已知取值不会产生虚假分支；
- 新增完整离散缺失值树，递归传播样本权重并在每个节点保存带权类别分布和分支概率；
- 预测属性缺失时汇总所有子树概率，遇到未见的非缺失值则回退当前节点分布；
- 新增17项测试，覆盖手算增益、自定义权重、权重守恒、缺失概率汇总、全缺失特征和非原地修改。

Phase 4 第五批已完成并纳入本批提交：

- 新增 `X @ w` 多变量线性投影节点与相邻投影中点阈值搜索；
- 新增最佳单轴划分作为透明基线，用加权基尼直接比较轴平行与斜线边界；
- 从坐标轴和类别均值差方向出发，逐坐标尝试正负步长并在无改进时缩小步长；
- 每个方向归一化并固定符号，阈值平局选择较小值，保证相同输入得到确定结果；
- 新增递归多变量树，支持非连续多分类标签、最大深度和叶节点最少样本约束；
- 新增16项测试，证明斜线边界优于单轴划分，并覆盖旋转数据、递归多分类、确定性和非原地修改。

基础补强第一批已完成并纳入本批提交：

- 新增 pandas `groupby` 命名聚合，明确 `size`、`nunique`、均值/最大值和 `reset_index()` 后的形状；
- 新增以成绩表为左表、学生表为右表的 inner/left/right/outer 连接与 `_merge` 来源审计；
- 用 `validate="many_to_one"` 拒绝重复学生主键，避免成绩行被静默复制；
- 新增内连接行数预测，按每个公共键的左右出现次数乘积解释多对多膨胀；
- 新增学生信息与成绩连接、未知编号拒绝、cohort/course分组统计和严格CSV保存完整任务；
- 新增15项测试，包含四种连接、未知/无成绩记录、重复键、非原地修改和逐字节报告输出。

基础补强第二批已完成并纳入本批提交：

- 新增显式特征列顺序选择，统一转换为浮点 `X:(n,d)` 和整数/浮点 `y:(n,)`；
- 专门拒绝标签列混入特征、缺失列、非数值/非有限数据和整数模式下的小数标签；
- 新增带列名Series的训练均值与尺度，转换时严格检查参数索引顺序；
- pandas 标准差显式使用 `ddof=0`，并与NumPy默认总体标准差逐项验证一致；
- 验证数值范围完全不同的验证表仍使用训练表统计量，常数列使用安全尺度1；
- 新增13项测试，覆盖列反序、`(n,)`/`(n,1)`、dtype、统计量隔离、非原地修改和逐字节预测输出。

基础补强第三批已完成并纳入本批提交：

- 新增稳定单列/多列排序，明确升降序参数与保留原索引/重置索引的区别；
- 新增严格长表转宽表，`(index, columns)` 键重复时给出冲突键而不是静默选值；
- 新增显式 `pivot_table` 聚合，只允许mean/sum/max/min，区分聚合语义与严格pivot；
- 新增 `melt` 宽表转长表，明确缺失格子保留或删除并稳定恢复规范行顺序；
- 清除pivot遗留的 `columns.name`，统一普通列和RangeIndex后再严格保存；
- 新增16项测试，覆盖稳定平局、四种聚合、缺失组合、长宽往返、非原地修改和逐字节宽表输出。

算法题第一批已完成并纳入本批提交：

- 新增学生记录稳定归并排序完整程序，分数降序且同分保持输入顺序；
- 手写递归拆分与稳定合并，覆盖严格记录数、唯一编号、整数分数和固定输出格式；
- 新增半开区间手写 `lower_bound` 与 `upper_bound`，输出边界、出现次数和不存在时插入位置；
- 二分题解析阶段验证非降序数组，并覆盖重复段、两数之间、范围两端和空数组；
- 两题均支持stdin/stdout以及输入文件/输出文件两种独立运行方式；
- 新增22项测试，包含函数性质、错误输入、子进程标准输入和逐字节文件输出。

算法题第二批已完成并纳入本批提交：

- 新增按首次出现顺序输出的整数频次表与保序去重；
- 新增确定性两数配对：从左到右扫描右端点，返回最早可发现答案并保留补数最早下标；
- 明确先查补数再写当前值，验证 `[3,3]` 使用两个下标且单个 `[3]` 不会复用自己；
- 输出统一包含unique、frequencies和pair三部分，不存在配对时输出NONE；
- 修复空序列文件解析，保留表示0个元素的结构空行而只去除一个文件末尾换行；
- 新增19项测试，覆盖负数、重复值、多答案规则、空序列、错误输入、stdin和逐字节文件输出。

算法题第三批已完成并纳入本批提交：

- 新增简单无向图严格解析、升序邻接表、确定性BFS/DFS和整图连通分量；
- 拒绝端点越界、自环、重复无向边和边行数不一致，边输入顺序不改变输出；
- 新增网格BFS最短路，固定上/左/右/下扩展并在入队时保存父节点；
- 输出最短距离及完整坐标路径，覆盖起终点相同、障碍封锁、不可达和多条同长路径；
- 公共最短路函数自身验证矩形网格、字符、坐标和障碍，不依赖调用者先运行解析器；
- 新增22项测试，两题stdin/stdout和输入文件/输出文件模式均由子进程及逐字节结果验证。

算法题第四批已完成并纳入本批提交：

- 新增非负整数权重简单有向图严格解析，拒绝负权、自环、重复边和端点越界；
- 新增最小堆Dijkstra，距离变小时压入新条目，弹出距离不等于当前最短值时统计并跳过陈旧项；
- 保存父节点恢复完整最短路径，邻接表与堆排序及“只在严格变短时更新”保证同长路径确定；
- 跑完整张图而非找到目标即停止，测试中直接验证至少一个陈旧条目被处理；
- 覆盖零权边、万亿级整数距离、不可达、起终点相同和边输入顺序变化；
- 新增14项测试，stdin/stdout和输入文件/输出文件模式均由子进程及逐字节结果验证。

算法题第五批已完成并纳入本批提交：

- 新增一维DP最少硬币完整程序，`dp[a]` 保存最少数量并用 `chosen_coin[a]` 恢复组合；
- 面额升序、数量平局时偏好更小当前硬币，恢复组合排序后输出，结果确定；
- 覆盖金额0、不可达、互异正面额校验和输入列表不原地修改；
- 新增二维DP最长公共子序列长度表，从右下角回溯并在长度平局时固定向上；
- LCS解析保留表示空序列的结构空行，覆盖无公共字符和多个最优序列；
- 新增18项测试，两题stdin/stdout和输入文件/输出文件模式均由子进程及逐字节结果验证。

算法题第六批已完成并纳入本批提交：

- 新增0/1背包完整程序，以 `dp[i][c]` 表示只看前 `i` 件物品时的最大价值；
- 选择当前物品时只读取上一行状态，从结构上保证每个下标最多使用一次；
- 从二维表右下角恢复所选下标，价值平局时排除较晚物品并优先保留较早下标；
- 明确对比一维0/1背包容量倒序与完全背包容量正序的复用差异；
- 覆盖零容量、零物品、没有物品可放、不可重复、价值平局、输入不修改和错误状态表；
- 新增26项测试，stdin/stdout和输入文件/输出文件模式均由子进程及逐字节结果验证。

Phase 5 第一批已完成并纳入本批提交：

- 建立第5章神经网络总览，只把已有证据的感知机子专题标为已验证；
- 统一 `X:(n,d)`、`y:(n,)`、`weights:(d,)` 与 `scores:(n,)` 的形状约定；
- 新增线性得分、零分归正类的阈值预测和固定样本顺序的逐样本感知机更新；
- 从零参数开始训练，一轮无更新时收敛，并用 `max_epochs` 处理线性不可分数据；
- 覆盖手算首轮更新、偏置不为零的分界线、线性可分收敛、XOR不可分反例和输入不修改；
- 新增25项测试和形状引导演示；覆盖矩阵已将感知机与尚未开始的多层网络/BP拆开记录。

Phase 5 第二批已完成并纳入本批提交：

- 新增单隐层网络参数约定：`W1:(d,h)`、`b1:(h,)`、`W2:(h,1)`、`b2:(1,)`；
- 实现固定种子可复现初始化、两层线性组合、稳定sigmoid和完整前向缓存；
- 新增显式 `y:(n,) -> (n,1)` 标签列化，拒绝损失函数静默接受错误的一维标签；
- 用演示和测试固定 `(n,1)-(n,) -> (n,n)` 的广播陷阱以及正确 `(n,1)` 误差形状；
- 直接从logits计算稳定二元交叉熵，极端正负1000仍保持有限数值；
- 新增36项测试，覆盖手算前向值、全部缓存形状、初始化可复现、输入独立副本和严格错误输入。

Phase 5 第三批已完成并纳入本批提交：

- 从平均二元交叉熵推导 `dz2=(probabilities-y)/n`，实现输出层 `dW2` 与 `db2`；
- 沿 `W2.T` 反传到隐层，实现sigmoid导数、`dW1` 和 `db1`，所有梯度形状与参数一致；
- 缓存必须恰好包含前向结果，且形状和数值都重新核对，拒绝陈旧或被篡改的缓存；
- 对 `W1、b1、W2、b2` 的每个标量做中心有限差分，并计算最大相对误差；
- 覆盖单样本、三隐单元、极端logit、输入不修改、错误标签/参数/缓存与非法差分步长；
- 新增22项测试和完整梯度形状演示；训练循环与XOR拟合仍保持未开始。

Phase 5 第四批已完成并纳入本批提交：

- 新增返回新参数字典的非原地梯度更新，逐键校验梯度有限性和参数同形状；
- 实现固定种子的单隐层全批量梯度下降，损失历史第0项明确记录更新前损失；
- `epochs=0` 返回初始参数和单项历史，普通训练历史长度固定为 `epochs+1`；
- 概率和标签均保留 `(n,1)` 输出神经元维，阈值相等时确定地归为1；
- 在线性数据上验证最终损失显著降低，并用4隐单元、固定种子和学习率拟合XOR；
- 新增25项测试和XOR端到端演示，同时明确具体配置成功不等于任意初始化都全局收敛。

Phase 5 第五批已完成并纳入本批提交：

- 新增可完整分析的一维双井目标、解析梯度和二阶导数，四次项保证两端趋向正无穷；
- 求出全部三个实驻点并用二阶导数分类为两个局部极小和一个局部极大；
- 比较两个极小点函数值，严格确定左侧深井是该目标的全局最小；
- 固定学习率从四个初值运行梯度下降，验证初值分别进入左右两个吸引域；
- 明确梯度接近零和损失下降都不构成到达全局最小的证明，并说明一维类比的边界；
- 新增22项测试和多初值演示，覆盖轨迹单调性、零步数、确定性、输入不修改和错误参数。

Phase 5 第六批已完成并纳入本批提交：

- 新增样本到RBF中心的平方欧氏距离矩阵，形状固定为 `(n,m)`；
- 实现高斯径向响应、可选偏置列和宽度正值校验，演示窄/宽基函数的局部性差异；
- 中心和宽度显式给定，将基函数表示与后续聚类选中心问题隔离；
- 无正则时用最小二乘处理列相关设计矩阵，有L2时只惩罚基函数权重而不惩罚偏置；
- 输出采用 `design:(n,m+1) @ weights:(m+1,) -> prediction:(n,)`，并严格拒绝列向量误广播；
- 新增18项测试和插值演示，覆盖重复中心、岭收缩、输入不修改、模型恢复和错误形状。

Phase 5 第七批已完成并纳入本批提交：

- 新增一维神经元网格SOM，输入 `X:(n,d)`、原型 `prototypes:(k,d)`，不使用监督标签；
- 实现样本到所有原型的平方距离和确定性BMU，距离平局选择最早神经元下标；
- Gaussian拓扑邻域输出 `(k,)`，通过 `neighborhood[:,None]` 与 `(k,d)` 更新量正确广播；
- 单步更新返回新原型数组，不修改旧原型，并限制学习率位于 `(0,1]`；
- 固定种子初始化，学习率和邻域半径指数衰减，最终值不得高于初始值；
- 新增25项测试和端到端演示，验证量化误差改善、远距簇映射、可复现性与严格错误输入。

Phase 5 第八批已完成并纳入本批提交：

- 新增严格二值、每行非零的ART1模式与二值类别交集原型约定；
- 实现选择分数 `|I∩w|/(alpha+|w|)`，按分数降序且平局优先较早类别；
- 警戒匹配率使用 `|I∩w|/|I|`，最高分候选失败后重置并继续检查后续候选；
- 共振类别采用按位交集快速学习，无类别通过时增量创建新类别；
- 高警戒值在固定样例中产生更细类别，达到最大类别数时明确失败，预测无共振返回 `-1`；
- 新增21项测试和低/高警戒对比演示，覆盖顺序敏感性、确定性、输入不修改和错误参数。

Phase 5 第九批已完成并纳入本批提交：

- 新增Elman参数形状 `Wx:(d,h)`、`Wh:(h,h)`、`bh:(h,)`、`Wy:(h,o)`、`by:(o,)`；
- 实现 `h_t=tanh(x_t@Wx+h_(t-1)@Wh+bh)` 和线性输出的单步递推；
- 序列前向返回 `states:(T+1,h)` 与 `outputs:(T,o)`，第0行明确保存初始状态；
- 验证当前输入相同而历史相反时输出不同，循环权重为0时历史依赖消失；
- 分段处理以上一段最终状态续接时与整段输出一致，重置为零状态时结果改变；
- 新增19项测试和上下文记忆演示；BPTT与训练保持未实现，不把固定参数实验冒充训练。

Phase 6 第一批已完成并纳入本批提交：

- 建立第6章目录与“间隔几何 → 对偶/SMO → 软间隔/核 → SVR”的分批路线；
- 新增线性超平面得分、零分归正类预测和 `(n,d)@(d,)->(n,)` 形状约定；
- 实现函数间隔、几何间隔与点到超平面的无符号距离，显式拒绝零权重向量；
- 验证 `(w,b)` 同乘正数时函数间隔缩放而几何间隔与预测保持不变；
- 对正确分离样本的超平面做规范化，使最小函数间隔为1，并找出当前最小间隔点；
- 新增19项测试和缩放演示；当前尚未求解最大间隔最优超平面，因此不冒称已训练SVM。

Phase 6 第二批已完成并纳入本批提交：

- 新增线性核Gram矩阵与SVM对偶目标，明确 `0 <= alpha_i <= C` 和 `sum(alpha*y)=0` 两类约束；
- 实现确定性的简化SMO，成对更新alpha、裁剪可行区间并同步更新偏置；
- 对二阶项退化的重复样本不做除零，而是比较可行区间两个端点的对偶目标；
- 从alpha恢复线性权重，定位支持向量，并提供决策值、标签预测和对偶历史；
- 新增KKT残差与原始软间隔目标检查，验证盒约束、等式约束和原始—对偶一致性；
- 新增20项测试和引导演示；这是仓库参考闭环，不代表学习者已经独立掌握SMO。

Phase 6 第三批已完成并纳入本批提交：

- 新增带符号间隔、逐样本hinge损失与最小松弛变量，明确二者数值相同但解释角度不同；
- 将样本区分为间隔外、间隔边界、间隔内、决策边界和误分类五种状态；
- 将alpha区分为非支持、自由支持和有界支持，并逐样本检查对应KKT关系；
- 分解 `0.5||w||^2 + C*sum(xi)` 的正则项、松弛惩罚与总目标；
- 在同一线性数据上比较多个C，验证增大C可以用更大的权重范数换取更少的训练松弛；
- 新增24项测试和多C演示，显式拒绝 `(n,1)`/`(n,)` 误广播和重叠状态容差；未替代学生作答。

Phase 6 第四批已完成并纳入本批提交：

- 新增线性、多项式与RBF核，统一 `K(X_query,X_train):(m,n)` 形状约定；
- 验证训练Gram矩阵对称且半正定、RBF对角线为1，并处理平方距离的浮点舍入下溢；
- 将确定性简化SMO扩展到预计算核矩阵，盒约束、等式约束和成对更新逻辑保持不变；
- 核化预测保存训练样本、alpha、标签、偏置和核参数，并支持仅使用支持向量计算；
- 在线性不可分XOR上验证线性核不能完美拟合，而二次多项式核与RBF核可以表达该边界；
- 新增24项测试和XOR演示，覆盖KKT、对偶历史、可复现性、参数边界与错误形状；未替代学生作答。

Phase 6 第五批已完成并纳入本批提交：

- 新增epsilon不敏感损失与管道内、边界、管道外三类回归残差状态；
- 将上下两侧对偶变量合并为 `beta=alpha-alpha_hat`，实现带L1绝对值项的SVR对偶目标；
- 通过确定性成对坐标优化保持 `sum(beta)=0`，比较区间端点、绝对值分段点和光滑区间驻点；
- 从自由支持向量或KKT可行上下界恢复偏置，支持线性核和RBF核回归；
- 验证仅使用非零beta支持向量的预测与完整展开一致，并处理常数目标无支持向量的偏置模型；
- 新增24项测试和epsilon管道演示，覆盖线性/弯曲目标、KKT、对偶历史、确定性和严格形状；未替代学生作答。

Phase 7 第一批已完成并纳入本批提交：

- 建立第7章贝叶斯分类器导航，按决策、参数估计、朴素贝叶斯、半朴素与贝叶斯网逐批推进；
- 新增类先验乘类条件似然后按证据归一化的后验概率计算；
- 实现0/1损失矩阵、一般非负代价矩阵、逐样本条件风险和最小风险动作；
- 验证0/1损失下最小风险等价于最大后验，而非对称错误代价可以改变分类决策；
- 推导二分类假阳性/假阴性代价对应的正类概率阈值，并固定“行动在行、真实类别在列”；
- 新增28项测试和决策差异演示，覆盖多样本、多分类、平局、概率归一化与错误输入；未替代学生作答。

Phase 7 第二批已完成并纳入本批提交：

- 新增类别先验、Bernoulli参数与有限类别分布的极大似然估计，并提供可选Laplace平滑；
- 实现单变量高斯均值与MLE方差，明确使用 `n` 分母即NumPy的 `ddof=0`；
- 实现多元高斯均值和 `centered.T@centered/n` 协方差，保留重复特征导致的真实秩亏；
- 新增Bernoulli对数似然，验证样本均值确实优于非最优候选参数；
- 区分MLE估计的先验/似然参数、贝叶斯公式得到的后验，以及尚未建设的参数MAP估计；
- 新增17项测试和ddof/协方差秩演示，覆盖手算、平滑、奇异协方差与错误输入；未替代学生作答。

Phase 7 第三批已完成并纳入本批提交：

- 新增离散朴素贝叶斯，按特征分别保存类别值域、类条件计数和平滑对数概率；
- Laplace分母显式包含未知值桶，使测试期未见取值仍得到有限联合对数分数；
- 使用对数先验与逐特征对数条件概率累加，并用减行最大值的稳定归一化输出后验；
- 新增高斯朴素贝叶斯，按类别/特征估计 `(K,d)` 均值与MLE方差；
- 对常数特征应用显式正方差下限，支持非连续标签并保持 `(n,K)` 分数/后验形状；
- 新增21项测试和离散未知值/高斯常数特征演示，覆盖手算、平局、输入不修改和错误边界；未替代学生作答。

Phase 7 第四批已完成并纳入本批提交：

- 新增AODE半朴素贝叶斯，让每个合格特征轮流作为超父，其余特征依赖类别与该父取值；
- 使用全局父取值频数阈值过滤稀疏SPODE，并为联合项、条件项和未知子值统一Laplace平滑；
- 在对数空间计算每个SPODE分数，再以稳定log-mean-exp汇总多个超父分类器；
- 查询期未见父取值不参与超父集合，但可作为未知子值；无合格父特征时精确回退朴素贝叶斯；
- 在相关离散特征上验证AODE联合分数与朴素独立分数不同，同时保持稳定归一化后验；
- 新增19项测试和父特征/回退演示，覆盖手算、列置换、非连续标签与错误输入；未替代学生作答。

Phase 7 第五批已完成并纳入本批提交：

- 新增二值贝叶斯网，父节点以有序元组定义并严格绑定CPT各轴语义；
- 使用Kahn算法生成确定性拓扑顺序，拒绝自环、未知父节点、重复父节点和有向环；
- 实现完整赋值的联合概率分解，并枚举所有二值赋值验证总概率为1；
- 实现证据概率求和与单变量二值精确后验，显式拒绝零概率证据；
- 在Cloudy-Sprinkler-Rain-WetGrass网络上核对16个赋值、证据概率与已知后验；
- 新增14项测试和经典网络演示，覆盖CPT形状/概率、父轴顺序、非法赋值与模型复制；未替代学生作答。

Phase 8 第一批已完成并纳入本批提交：

- 建立第8章集成学习导航，按Boosting、Bagging/随机森林、结合策略与多样性推进；
- 新增加权决策树桩，枚举特征、相邻中点/两端阈值和正负极性，并固定确定性平局规则；
- 实现AdaBoost加权错误率、分类器权重和指数样本权重更新，错分样本相对权重上升；
- 实现多轮强分类分数与阈值预测，完美树桩有限alpha后提前停止，随机质量树桩不加入；
- 验证多树桩拟合、指数训练损失逐轮下降、权重逐轮归一化与训练输入不修改；
- 新增20项测试和首轮权重转移演示，覆盖常数分类器、极性、平局、错误输入与确定性；未替代学生作答。

Phase 8 第二批已完成并纳入本批提交：

- 新增固定种子的Bootstrap采样矩阵，明确可重复训练下标、唯一袋内样本与袋外补集；
- 每个采样袋训练确定性单特征树桩，单类别袋使用常数分类器，最终硬投票平局归+1；
- 实现基学习器预测矩阵、平均投票分数与Bagging标签预测；
- 对每个训练样本只汇总未抽中它的学习器，返回OOB分数和覆盖计数；
- 无OOB覆盖样本保留NaN并从OOB准确率排除，全无覆盖时明确报错；
- 新增19项测试和Bootstrap/OOB演示，覆盖手算补集、种子复现、投票平局、覆盖掩码与错误输入；未替代学生作答。

Phase 8 第三批已完成并纳入本批提交：

- 在Bootstrap样本随机性之外新增每学习器无放回随机候选特征子集；
- 单节点树桩只在自己的候选子集中选最佳划分，明确其等价于完整森林的单节点特例；
- `max_features=d` 时所有候选子集覆盖全特征，固定种子同时复现Bootstrap与特征子集；
- 实现基学习器预测矩阵、森林平均投票和确定性平票标签；
- 新增平均成对预测相关系数，排除无方差常数预测对，全无定义时返回NaN；
- 新增19项测试和随机候选特征演示，覆盖候选约束、多种特征使用、相关性、复现性与错误输入；未替代学生作答。

Phase 8 第四批已完成并纳入本批提交：

- 新增等权/非负归一化加权回归平均，以及分类硬投票和概率软投票；
- 固定硬投票选择排序较早标签、软投票选择较早类别下标的确定性平局规则；
- 实现两个分类器相对真实标签的n11/n00/n10/n01四格计数；
- 实现Q统计量、正确性相关系数、分歧度与双错率，分母为0时保留NaN；
- 实现等权回归的逐样本误差—分歧恒等式，验证个体均方误差减歧义等于集成平方误差；
- 新增19项测试和多样性/恒等式演示，覆盖手算、未定义边界、输入不修改与错误权重；未替代学生作答。

Phase 9 第一批已完成并纳入本批提交：

- 建立第9章聚类导航，按距离/评价、原型、层次和密度聚类逐批推进；
- 新增Minkowski距离（含L1/L2）与无序类别Hamming距离矩阵，函数不隐式标准化；
- 实现任意标签簇的簇内平方和SSE和逐样本轮廓系数，单例簇按约定记0；
- 实现真值/预测划分的样本对四格计数与Rand指数；
- 根据列联表组合数实现调整Rand指数，支持负值、标签置换及全单簇/全单例退化分区；
- 新增21项测试和内部/外部指标演示，覆盖手算、距离性质、单例簇、标签置换与错误输入；未替代学生作答。

Phase 9 第二批已完成并纳入本批提交：

- 新增样本到中心的 `(n,k)` 平方距离矩阵和按行最近中心分配，距离平局选较早中心；
- 实现固定种子K-means++，首中心随机、后续按最近中心平方距离采样，并拒绝k超过不同坐标数；
- 实现均值中心更新、重新分配、中心移动/标签不变收敛条件与初始/逐轮SSE历史；
- 空簇依次重置到当前分配误差最大的未使用样本，避免空均值产生NaN；
- 验证单簇退化为全局均值、重复样本坐标不重复选中心、最终SSE可重新计算；
- 新增23项测试和K-means++/SSE演示，覆盖手算、平局、空簇、确定性、输入不修改与错误参数；未替代学生作答。

Phase 9 第三批已完成并纳入本批提交：

- 新增LVQ1监督原型学习，明确其使用类别标签而K-means不使用标签；
- 实现样本到原型平方距离、最近原型及距离平局选择较早原型；
- 赢家标签正确时按学习率向样本靠近，错误时沿相反方向远离，且只修改赢家副本；
- 每类无放回分层初始化指定数量原型，固定种子并保持非连续原始标签；
- 实现固定样本顺序、指数学习率衰减、逐轮正确赢家计数和独立原型历史；
- 新增18项测试和靠近/远离演示，覆盖手算、初始化、历史副本、训练预测与错误输入；未替代学生作答。

Phase 9 第四批已完成并纳入本批提交：

- 新增欧氏样本距离矩阵和single/complete/average三种簇间链接；
- 从样本下标单例簇开始，用Kahn无关的直接凝聚流程合并最近活动簇；
- 距离平局按左右簇编号字典序选择，新簇编号依次递增并记录左右/新编号、距离和成员；
- 支持目标簇数1到n，最终标签按各簇最小成员排序而非内部新簇编号；
- 验证三种标准链接合并距离非下降，完整合并历史长度为n-k；
- 新增25项测试和三链接演示，覆盖手算、平局、成员标签、确定性、输入不修改与错误簇定义；未替代学生作答。

Phase 9 第五批已完成并纳入本批提交：

- 新增欧氏距离和闭区间 `distance <= eps` 邻域矩阵，明确MinPts统计包含样本自身；
- 根据邻域计数区分核心点，并按样本下标从小到大启动确定性队列扩展；
- 核心点沿密度可达链连通，未归类邻域样本作为边界点吸收，其余样本保留为噪声标签 `-1`；
- 返回核心/边界/噪声掩码、邻域计数、邻域成员和簇成员，便于按中间状态学习而非只看最终标签；
- 验证 `eps` 边界包含、自身计数、链式扩展、全噪声、`min_samples=1`、大邻域与确定性；
- 新增26项测试和邻域到分类的逐步演示；未替代学生作答。

Phase 10 第一批已完成并纳入本批提交：

- 建立第10章导航，以kNN、MDS、PCA、核化、流形和度量学习为建设顺序；
- 新增查询集到训练集的 `(n_query,n_train)` 欧氏距离矩阵和逐行稳定最近邻选择；
- 实现kNN均匀/距离加权分类，标签票数或权重平局时选择排序较早类别；
- 实现kNN均匀/距离加权回归，距离为0时只使用完全重合的训练样本；
- 明确单查询仍保持二维、距离平局按训练下标、`k` 范围和目标一维形状约束；
- 新增27项测试和距离行列/邻居/预测演示，覆盖手算、非连续标签、零距离、输入不修改和错误输入；未替代学生作答。

Phase 10 第二批已完成并纳入本批提交：

- 新增欧氏样本距离矩阵和 `B=-1/2 J D^2 J` 双中心化内积恢复；
- 实现对称特征分解、特征值降序、特征向量确定性符号和非负谱坐标构造；
- 返回完整谱、选中谱、特征向量和Gram矩阵，保留明显负特征值用于诊断非欧氏距离；
- 实现低维坐标的归一化stress，覆盖全维距离重构和降至一维的信息损失；
- 验证中心化Gram与原坐标Gram一致、矩形已知谱、直线一维精确恢复、平移旋转不变和单样本退化；
- 新增22项测试和距离到坐标的逐步演示；修正演示中浮点负零展示，未替代学生作答。

Phase 10 第三批已完成并纳入本批提交：

- 新增按 `axis=0` 求特征均值和显式中心化，输出均值、协方差与完整谱；
- 实现样本协方差对称特征分解、主轴降序和确定性方向符号；
- 实现PCA投影 `(X-mean) @ components.T`、逆变换和逐元素重构MSE；
- 返回选中/全部解释方差与解释方差比，常数数据和单样本安全返回零方差；
- 验证已知矩形主轴/方差/比例、主轴正交、平移不变、全维精确重构和降维信息损失；
- 新增23项测试和形状/解释方差/重构演示，覆盖输入不修改和参数形状错误；未替代学生作答。

Phase 10 第四批已完成并纳入本批提交：

- 新增线性、多项式和RBF核矩阵，统一校验核参数与交叉核矩阵形状；
- 实现训练核矩阵四项中心化，保存训练列均值和总均值供新样本复用；
- 实现中心化Gram谱分解、正特征值筛选、确定性方向和训练坐标；
- 实现新样本 `(n_new,n_train)` 核矩阵中心化与谱投影，训练集再投影可恢复训练坐标；
- 验证线性核PCA坐标Gram等于普通中心化坐标Gram、RBF谱为正且坐标中心化；
- 新增20项测试和RBF核中心化/谱/再投影演示；未替代学生作答。

Phase 10 第五批已完成并纳入本批提交：

- 新增逐行稳定k近邻图，排除自身并以有向近邻并集构造无向加权图；
- 实现支持无穷非边的Floyd-Warshall全源最短路，以图距离近似流形测地距离；
- 将有限测地距离送入经典MDS，返回坐标、欧氏距离、图、测地距离和谱；
- 近邻图不连通时明确报错并提示增大邻居数，不把无穷距离替换成任意大数；
- 验证弧线端点测地距离大于弦长、完全图退化为欧氏距离、断图和平局/对称化规则；
- 新增21项测试和弧线测地距离演示；未替代学生作答。

Phase 10 第六批已完成并纳入本批提交：

- 新增Mahalanobis距离矩阵，显式要求度量矩阵与特征数匹配、对称且半正定；
- 建立同类平方距离、异类间隔hinge和相对单位阵正则组成的教学目标；
- 实现目标梯度、特征值裁剪PSD投影和保证目标不增的回溯步长；
- 返回学习矩阵、目标历史和迭代次数，展示不同间隔如何改变特征方向权重；
- 验证单位阵等价欧氏距离、对角缩放、PSD最小特征值、目标下降与确定性；
- 新增18项测试和类内/类间方向权重演示；这是代表性小模型，不宣称替代所有度量学习算法。

Phase 11 第一批已完成并纳入本批提交：

- 建立第1章基本术语说明，区分样本/特征/标记、分类/回归/聚类、假设与误差；
- 对有限二分类输入域穷举 `2^n` 个目标映射，显式构造假设空间；
- 根据部分观测筛选版本空间，展示训练一致性通常不能唯一确定未见点；
- 用相邻预测变化次数给出可计算的归纳偏好示例，并固定平局规则；
- 穷举所有目标函数，验证两个不同未见点规则在NFL前提下的平均错误均为0.5；
- 新增17项测试和假设/版本空间/NFL演示，并明确NFL适用前提；未替代学生作答。

Phase 11 第二批已完成并纳入本批提交：

- 新增候选隐藏激活与当前残差的逐列绝对相关系数，常量分母安全记0；
- 从线性输出基线出发，每轮选择相关性最大候选并冻结其输入权重；
- 新隐藏单元接收原始特征和此前隐藏激活，输入宽度按 `d,d+1,...` 增长；
- 每次追加隐藏激活后重新拟合输出层，保存选择相关性和训练MSE历史；
- 模型预测按保存顺序重建全部隐藏激活，验证最终预测MSE与记录一致；
- 新增14项测试和结构增长演示；明确随机候选池是原理实验而非完整候选权重优化。

Phase 11 第三批已完成并纳入本批提交：

- 新增最多20个二值单元的确定性状态枚举，小模型可精确穷举 `2^d` 个状态；
- 实现对称零对角连接下的能量、温度缩放和稳定softmax精确概率；
- 从精确分布计算单元均值、二阶矩与对数配分函数；
- 实现单元条件激活sigmoid，并用精确联合概率比验证公式；
- 实现固定单元顺序、固定随机种子的Gibbs采样轨迹，显式区分MCMC样本与精确分布；
- 新增16项测试和能量/精确分布/采样演示；暂不伪装成RBM完整训练。

Phase 11 第四批已完成并纳入本批提交：

- 在已有Elman状态递推和分段续接上新增完整单序列MSE时间反向传播；
- 从最后时刻向前累加 `Wx、Wh、bh、Wy、by` 梯度并返回初始状态梯度；
- 新增逐参数中心有限差分检查，五组解析梯度在小网络上误差低于阈值；
- 新增完整序列梯度下降训练、初始/逐轮损失和裁剪前全局梯度范数；
- 支持可选全局范数裁剪，验证延迟目标产生循环权重梯度、训练确定性和输入不修改；
- 原有19项前向测试与新增12项BPTT测试共31项通过；仍明确区分完整BPTT与尚未单列的截断BPTT。

Phase 12 第一批已完成并纳入本批提交：

- 根据2016年第1版目录审计确认第7.6节EM此前遗漏，不再以覆盖矩阵全绿代替目录核验；
- 新增二项混合隐变量实验，在对数空间计算两成分责任度并逐行归一；
- 使用责任度软计数更新混合比例和两枚硬币正面概率；
- 稳定计算含二项组合项的观测数据对数似然，逐轮记录并验证不下降；
- 展示初始化、局部驻点和成分标签整体交换的边界；
- 新增13项测试和E/M步演示；高斯混合聚类留在第9章独立建设。

Phase 12 第二批已完成并纳入本批提交：

- 根据第9.4节原型聚类正文审计确认高斯混合聚类此前遗漏；
- 新增完整协方差多元高斯对数密度，以Cholesky分解避免显式求逆和下溢；
- E步在对数空间计算逐样本责任度和观测似然，M步更新权重、均值与完整协方差；
- 使用K-means++、短程硬分配和分簇协方差初始化，修复全局协方差初始化导致的交错局部解；
- 对协方差加入正对角正则，并最终按均值字典序固定成分标签；
- 新增19项测试和二维软聚类演示，覆盖似然、正定性、单成分、确定性和局部初始化问题。

Phase 12 第三批已完成并纳入本批提交：

- 根据第10.5节正文审计确认流形学习除Isomap外还明确包含LLE；
- 新增稳定逐行k近邻下标，距离平局选择较小样本下标且排除自身；
- 解带和为1约束的局部线性重构权重，按局部Gram迹加正则处理共线与 `k>d`；
- 构造 `(I-W)^T(I-W)` 并取跳过常数方向后的最小特征向量；
- 显式中心化和单位尺度约束低维坐标，固定特征向量符号保证可复现；
- 新增20项测试和抛物线局部重构演示，覆盖平移/旋转不变、PSD、奇异邻域和错误输入。

Phase 12 第四批已完成并纳入本批提交：

- 根据第10.6节正文审计确认教材以NCA说明度量学习，原有成对约束实验不能替代；
- 新增线性变换后逐样本留一概率近邻矩阵，对角为0且每行和为1；
- 目标直接最大化随机选择到同类近邻的平均概率；
- 实现对方阵变换的解析梯度，并用逐元素中心有限差分验证；
- 使用回溯梯度上升保证目标历史不下降，返回 `A.T@A` 半正定度量；
- 修正过强噪声造成softmax饱和的教学数据，最终正确概率显著提升且噪声方向权重下降；新增14项测试。

Phase 12 第五批已完成并纳入本批提交：

- 根据第5.6节正文审计补充多隐层训练、无监督逐层预训练和权共享；
- 用线性PCA自编码层实现透明的逐层无监督预训练，每层保存自己的均值和主方向；
- 支持多层编码重放，记录逐层编码形状和各层局部重构MSE；
- 明确“预训练表示后只训练监督头”不等于更新所有层的完整微调；
- 手写有效一维多滤波器卷积，验证相同核跨位置复用和响应随局部模式平移；
- 计算共享卷积与逐位置局部连接参数量；新增13项测试和逐层/权共享演示。

Phase 12 第六批已完成并纳入本批提交：

- 根据第5.5.6节正文审计补充受限Boltzmann机和对比散度，避免只用精确小型Boltzmann机代替教材内容；
- 实现二值可见层/隐层的双向条件概率，并用稳定sigmoid处理极端输入；
- 实现CD-1的正相、一步Gibbs负相、采样统计量梯度和非原地参数更新；
- 提供可复现训练历史，同时明确随机CD单步重构误差不保证单调下降；
- 新增14项测试，覆盖条件概率、采样状态、手算梯度、参数校验、可复现性和小型训练行为。

Phase 12 第七批已完成并纳入本批提交：

- 以2016年1月第1版目录为基准建立第1～10章逐节覆盖审计，不再把“每章有模块”等同于“教材无遗漏”；
- 为每个正文小节记录现有证据、已验证/部分覆盖/笔记待补状态和明确后续动作；
- 识别出第7.5节贝叶斯网学习与Gibbs推断、第7.4节TAN、第6.6节表示定理/KLDA三个真实方法缺口；
- 记录第1.5～1.6节历史与应用知识卡缺口，同时明确阅读材料不强行编造成算法；
- 修正第10章总览中“六个专题”的旧计数为当前八个专题。

Phase 12 第八批已完成并纳入本批提交：

- 扩展第7.5节二值贝叶斯网，增加已知DAG下的CPT计数学习和可选Laplace平滑；
- 实现数据对数似然、自由参数计数与需最小化的BIC评分；
- 从空图出发，沿指定拓扑顺序贪心加边，保证无环并只接受BIC严格下降；
- 增加顺序/同父/V型结构、解释消除、道德图与有向分离知识卡；
- 实现基于本结点及直接子结点因子的Markov毯条件概率和带证据Gibbs推断；
- 用小网络将Gibbs估计与枚举精确后验对照；原有14项测试与新增16项测试共30项通过。

Phase 12 第九批已完成并纳入本批提交：

- 补齐第7.4节TAN，按类别加权计算任意特征对的条件互信息；
- 构建对称零对角权重矩阵，并用确定性Prim过程生成指定根的最大带权生成树；
- 在树结构上用Laplace平滑估计类别先验、根条件概率和非根的类别/父特征条件概率；
- 在对数域累加TAN分数，支持非连续整数属性值与类别标签，并显式拒绝训练时未见属性值；
- 用“单特征边际相同、条件依赖不同”的反例验证TAN能捕获朴素贝叶斯遗漏的信息；新增22项测试。

Phase 12 第十批已完成并纳入本批提交：

- 补齐第6.6节表示定理知识卡，统一解释核模型为何能写成训练样本核函数的有限线性组合；
- 实现线性、多项式与RBF查询—训练核矩阵，并固定 `(m,n) @ (n,) -> (m,)` 形状主线；
- 在核坐标中构造类间均值差外积与类内中心化散度矩阵，验证对称半正定；
- 用正则化伪逆求二分类KLDA系数，以两类投影均值中点判别并保留非连续标签；
- 用RBF-KLDA拟合线性核类均值差为零的XOR反例，明确KLDA系数不必像SVM一样稀疏；新增18项测试。

Phase 12 第十一批已完成并纳入本批提交：

- 对第1.5节建立“推理—知识—学习范式—统计学习—规模化应用”的原创理解框架和时间索引；
- 不把发展史写成人物年份背诵表，而用每次范式变化试图解决的瓶颈组织内容；
- 对第1.6节建立“样本—特征—监督—输出—代价—部署”六问应用拆解法；
- 增加分类、回归、聚类、排序、医学筛查与设备预警场景，并连接数据泄漏和分布漂移；
- 学生问题与参考思路分离，要求换新场景迁移，不把参考措辞当作唯一答案。

Phase 12 第十二批已完成并纳入本批提交：

- 正文细项复核发现第2.3节已有代价敏感错误但缺少代价曲线，纠正“大标题已覆盖”的误判；
- 实现正类基率与假正/假反代价到正例概率代价横轴的映射；
- 将每个ROC点转换为 `(1-x)*FPR + x*FNR` 归一化代价线；
- 对全部阈值线逐点取下包络并计算数值面积，明确不能取平均线；
- 新增17项测试，与原有性能度量12项合计29项通过。

Phase 12 第十三批已完成并纳入本批提交：

- 正文细项复核确认第9.3节不能用Minkowski/Hamming代表全部距离计算，补齐有序/无序属性区别；
- 按无序属性值的组别条件分布实现VDM，并明确使用真值类别拟合时属于监督信息；
- 实现显式特征权重的Minkowski距离，以及数值有序属性与VDM无序属性的混合距离；
- 支持字符串类别和可选Laplace平滑，显式拒绝训练时未见值而不把编码整数误当连续数；
- 增加非度量相似性知识边界，提醒依赖三角不等式的算法性质不能直接沿用；
- 新增21项测试，与原有距离/聚类指标21项合计42项通过。

Phase 12 第十四批已完成并纳入本批提交：

- 补齐第8.5.3节多样性增强，将机制明确分成数据样本、输入属性、输出表示和算法参数四类；
- 实现每个基学习器的Bootstrap下标矩阵和无放回、升序随机特征子空间；
- 实现精确翻转指定比例位置的多类别标签副本，显式限制标签来自原类别集；
- 为不同初始化/超参数入口生成独立uint32种子，并由单个主种子派生完整可复现扰动计划；
- 文档记录各机制适用边界，强调多样性增大不保证个体准确性或集成性能提升；
- 新增22项测试，与原有结合/多样性19项合计41项通过。

Phase 12 第十五批已完成并纳入本批提交：

- 正文细项复核发现第2.3.2节二分类P/R/F1不能替代多分类宏平均与微平均；
- 对每个类别执行一对其余计数，返回逐类TP/FP/FN与P/R；
- 按教材口径先平均各类P/R再求宏F1，并先汇总计数再求微P/R/F1；
- 验证单标签多分类中微P、微R、微F1与accuracy相等，同时支持显式类别顺序和缺席类别；
- 实现P-R平衡点BEP：有离散交点直接取值，分数并列跨过交点时在线段上插值；
- 新增14项测试，与原有性能度量/代价曲线29项合计43项通过。

Phase 12 第十六批已完成并纳入本批提交：

- 正文细项复核确认第9.2节外部指标不能只用RI/ARI代表，补Jaccard与Fowlkes–Mallows；
- 复用样本对四格计数实现JC/FMI，并在没有任何同簇对时返回NaN而不伪造满分；
- 按教材的簇内平均成对距离和簇中心距离实现Davies–Bouldin指数；
- 按最小簇间样本距离与最大簇内直径实现Dunn指数；
- 验证分离程度、平移、标签重命名、全单点簇和中心重合簇等边界；
- 新增10项测试，与已有距离/指标42项合计52项通过。

Phase 12 第十七批已完成并纳入本批提交：

- 正文细项复核确认第8.4节不能只用平均/投票代表学习法，补齐Stacking训练闭环；
- 构造互不重叠且完整覆盖样本的K折验证下标，并保持次级特征回填到原样本顺序；
- 每一折重新创建基模型，只用折外预测构造形状为`(n,B)`的次级训练数据；
- 另用全部训练集重训部署时的基模型，避免把折内临时模型误用于新样本预测；
- 用不惩罚截距的NumPy岭回归完成次级组合器，并提供完整训练/预测接口；
- 用会记忆训练样本的模型直观验证训练内预测泄漏，新增引导演示和15项测试。

Phase 12 第十八批已完成并纳入本批提交：

- 复核现有三道综合题后不重复建设回归/分类CSV流水线，新增无监督K-means完整任务；
- 严格读取带唯一ID的CSV，以空白表示缺失并拒绝显式NaN/Inf、整行缺失和异常表头；
- 只用训练集拟合缺失值均值与安全标准差，模型保存均值、标准差和标准化空间中心；
- 对每个候选k进行多次K-means++初始化，先按inertia选同k最佳运行，再按轮廓系数跨k选模；
- 规范化簇编号，保存恢复后对含缺失值的测试CSV分簇，输出固定格式的标签与诊断文件；
- 学生入口拆为六关并配迁移题，新增16项参考测试，不据此标记学生掌握。

Phase 12 第十九批已完成并纳入本批提交：

- 建立首套120分钟、150分基础闭环诊断卷，按20/30/70分钟分配CSV统计、BFS与NumPy逻辑回归；
- 明确考试隔离流程：计时中只查看试卷、数据和学生入口，不查看参考、期望输出、测试或Git历史；
- 第1题覆盖严格CSV、均值填补后的总体标准差和固定格式，第2题覆盖确定性BFS路径恢复；
- 第3题覆盖训练集标准化、稳定sigmoid、带L2逻辑回归、验证指标、模型保存恢复与测试预测；
- 建立150分自动初筛器，会替换输入检查边界、模型隔离和异常退出，同时明确最终仍需源码复核；
- 三题参考解、期望输出与16项验收测试通过；学习者尚未计时作答，不记录成绩或掌握。

Phase 12 第二十批已完成并纳入本批提交：

- 新增`foundation_coverage.md`，把基础能力的仓库证据与学习者掌握状态分开审计；
- 补齐`.npy/.npz`基础专题，覆盖精确目标路径、`allow_pickle=False`、严格键集合和标量/向量形状；
- 模型包同时保存训练均值、尺度、权重与截距，恢复后通过完整预处理预测验证；
- 补齐类别特征训练词表、序号编码、one-hot块、未知类别和数值特征拼接；
- 用UTF-8 JSON持久化特征名、词表、未知桶开关与格式版本，避免只保存权重后丢失列语义；
- 两个专题均保留学生starter、引导演示、迁移题和损坏文件/未知类别边界测试，不据此记录个人掌握。

Phase 12 第二十一批已完成并纳入本批提交：

- 将命令行完整程序从综合题中的隐含要求拆成零基础独立专题；
- 用`load → compute → save → run_pipeline → build_parser → main`六层结构隔离输入、算法、输出和参数解析；
- `main(argv)`支持测试显式参数，不修改全局`sys.argv`，模块导入时不自动读文件或退出；
- 区分argparse参数错误、预期输入错误和编程错误，只捕获`OSError/ValueError`并返回状态2；
- 验证输出目录、UTF-8、LF换行、逐字节报告、真实子进程退出码和标准错误；
- 新增14项测试、学生分步入口、引导演示与五道迁移题，不据此记录个人掌握。

Phase 12 第二十二批已完成并纳入本批提交：

- 补齐mini-batch索引计划，明确不丢尾批时每个epoch恰好覆盖每个样本一次；
- 只打乱一组下标并同步提取`X/y`，用可手算对应关系检测形状正确但标签错位的静默错误；
- 整个训练计划只创建一次`default_rng`，验证同种子完整复现且不同epoch不会反复得到相同顺序；
- 明确`drop_last`、尾批、`batch_size>n`和全部样本被丢弃的边界；
- 实现线性回归半MSE解析梯度、有限差分校验、全批单步等价和多轮mini-batch拟合；
- 新增19项测试、学生入口、引导演示和迁移到逻辑回归的练习，不据此记录个人掌握。

Phase 12 第二十三批已完成并纳入本批提交：

- 正文复核确认教材第4.4.1节默认按信息增益选择连续阈值，保持该主线不变；
- 补连续二分增益率和平均信息增益过滤作为第4.2.2节衔接扩展，并明确不冒充教材硬性缺口；
- 真正补齐第4.4.2节连续缺失路径：候选阈值仅来自非缺失正权重样本；
- 连续缺失增益先在已知样本计算，再乘已知权重占比；缺失样本按左右已知质量传播且逐样本守恒；
- 连续属性在后代节点保持可用，预测缺失时沿左右子树加权类别概率，范围外有限值仍确定分支；
- 保持原接口默认所有特征为离散，新增连续/混合类型入口和17项连续缺失测试；第4章三组相关测试共55项通过。

Phase 12 第二十四批已完成并纳入本批提交：

- 修正第3章“仅建设线性回归”和第5章“仅完成感知机”等已经过时的章节导航，并补充仓库证据不等于个人掌握的边界；
- 复核第5.3节与习题5.5，确认原有训练循环只覆盖每轮全训练集更新一次的累积BP；
- 新增标准BP逐样本前向、反向和立即更新，同时保留原`train_network`兼容入口并增加含义明确的累积BP入口；
- 样本访问计划支持固定顺序或打乱，只创建一个随机数生成器跨轮使用，并将网络初始化种子与顺序种子分离；
- 两种算法统一按完整epoch记录全训练集损失，明确一轮内更新次数不同，不能只看相同epoch数作不加说明的效率比较；
- 验证标准BP的顺序敏感性、累积BP的行重排不变性、单样本时两者等价、确定性、输入不修改和学生入口隔离；本专题两组测试共49项通过。

Phase 12 第二十五批已完成并纳入本批提交：

- 新建异常值独立基础专题，明确异常值只表示统计上可疑，不自动等同于录入错误或应删除样本；
- 仅用训练集按列拟合Tukey IQR上下界，输出与数据同形状的布尔掩码，并分别统计逐特征异常数和含异常样本；
- 可选逐元素裁剪返回新数组，不提供默认删行接口，避免无依据删除稀有类别或改变标签分布；
- 新增训练集中位数/IQR稳健缩放，零IQR列使用安全尺度1，但诊断边界仍保持零宽度以暴露测试分布变化；
- 通过测试集整体漂移反例验证重新拟合会掩盖异常并造成数据泄漏，同时覆盖边界相等、常数列、广播形状、输入不修改和非法状态；
- 新增讲解、学生六步入口、引导演示、六道迁移题和36项测试，不据此记录个人掌握。

Phase 12 第二十六批已完成并纳入本批提交：

- 新建混合数值/类别特征的无OJ分类综合任务，严格区分数值空白、显式非有限值、类别空白、ID和二进制标签；
- 只用训练集拟合数值均值填补、填补后均值/安全标准差及逐类别列词表，验证/测试新类别进入各自未知桶；
- 固定设计矩阵为两列数值标准化特征加两个one-hot块，训练NumPy对数几率回归并在验证集计算accuracy、precision、recall、F1和AUC；
- 将人工可审计的特征语义和词表保存为严格版本化UTF-8 JSON，将填补/缩放/权重/截距/阈值保存为`allow_pickle=False`的NPZ；
- 训练与预测拆为两个CLI子命令，预测进程只接收测试CSV、JSON和NPZ，并在词表决定的设计宽度与权重长度不一致时拒绝运行；
- 样例包含数值缺失、验证/测试未知类别和单独期望输出；44项测试覆盖泄漏、状态损坏、对象数组、单行形状、独立进程与逐字节输出；不据此记录个人掌握。
- 将本地演示产物目录`work/`加入忽略规则，防止临时模型和预测文件误提交到仓库。

Phase 12 第二十七批已完成并纳入本批提交：

- 正文细项复核确认第3.5节不仅包含OvR/OvO，还以ECOC作为MvM最常见实现，原有覆盖记录过度概括；
- 生成OvR二元编码矩阵和OvO三元编码矩阵，统一展示每列二分类任务以及0停用类只在该列排除样本的语义；
- 支持任意合法二元/三元编码逐列构造0/1训练目标，并复用透明的类别中心线性打分器训练全部编码位；
- 将二分类分数确定映射为-1/+1预测码，按海明或欧氏距离解码回升序原标签，距离平局保持确定性；
- 计算最小海明码距，并仅对不含0的二元码给出理想独立位错误下的保证纠错位数，明确长码会增加分类器开销且理论性质不等于实际准确率；
- 用7位四类二元码验证最小码距4和单比特纠错，用OvR/OvO特例验证与原实现参数和预测一致；相关两组共57项测试，不据此记录个人掌握。

Phase 12 第二十八批已完成并纳入本批提交：

- 正文细项复核确认第3.2节在普通线性回归后还明确引出对数线性回归、单调可微联系函数和广义线性模型视角；
- 新增统一线性预测子`eta=X@w+b`，实现恒等、log与logit联系及其逆联系，保持`eta/mu`均为一维样本向量；
- logit逆联系按正负区间稳定计算，log逆联系显式检测指数溢出，并拒绝log非正数和logit边界0/1；
- 对严格正目标实现`log(y)`最小二乘与`exp(eta)`预测，验证精确指数关系、秩亏设计和目标乘常数时的截距平移；
- 区分原始尺度MSE与联系函数尺度MSE，并明确变换后OLS不等于包含响应分布、方差函数和极大似然的全部统计GLM；
- 新增讲解、学生七函数入口、引导演示、八道迁移题和33项测试；与普通线性回归共43项相关测试，不据此记录个人掌握。

Phase 12 第二十九批已完成并纳入本批提交：

- 正文细项复核确认第3.3节在一阶梯度后还以牛顿法给出二阶迭代，原专题只有梯度下降；
- 以参数顺序`[w_1,...,w_d,b]`构造增广设计矩阵和`(d+1,d+1)`Hessian，验证对称、半正定与梯度有限差分；
- L2只加到权重对角项而不正则化截距，数值阻尼另加到整个Hessian，明确两者改变目标/数值求解的不同作用；
- 通过`(H+damping*I) direction=gradient`求牛顿方向，并用回溯步长只接受有限且非增的损失；
- 用重复特征制造秩亏，验证无L2/无阻尼时明确失败、正阻尼时可解；稳定训练在演示数据上以6步达到低于100步梯度下降的损失，但不据此声称任意规模都更快；
- 新增26项二阶测试和更新后的引导演示；与原对数几率回归共38项相关测试，不据此记录个人掌握。

Phase 12 第三十批已完成并纳入本批提交：

- 正文细项复核确认第3.4节在二分类Fisher方向后继续给出多分类LDA与至多`K-1`维监督降维，原专题只覆盖0/1标签；
- 计算升序原类别、全局/逐类均值、类内`Sw`、按类样本数加权的类间`Sb`和总散度`St`，验证`St=Sw+Sb`；
- 对`Sw+regularization*I`做对称特征分解与白化，再对白化后的`Sb`求降序广义特征方向，避免直接分解非对称`pinv(Sw)@Sb`；
- 投影列归一化并固定符号，默认只返回正广义特征值对应的有效方向，维数不超过`min(d,K-1)`，显式索要超过数据有效秩时拒绝；
- 用投影空间最近类中心返回非连续原标签，距离平局选升序首类，并明确这只是监督降维的透明预测器；
- 覆盖平移不变、重复特征、零类内散度、类中心共线、广义特征残差和迹比尺度不变；新增34项测试，与二分类共45项，不据此记录个人掌握。

Phase 12 第三十一批已完成并纳入本批提交：

- 正文细项复核确认第6.3节不仅使用核技巧，还给出对称函数对应任意有限核矩阵半正定的判据、拉普拉斯/Sigmoid候选与三种核组合闭包，原专题只会调用三种核；
- 统一实现线性、多项式、RBF、拉普拉斯与Sigmoid候选核，保持查询—参照核矩阵形状为`(m,n)`；
- 对方阵计算对称误差、对称部分特征值和容差内PSD结论，并提供`c.T@K@c`最小反例验证；
- 对有限PSD Gram矩阵用特征分解构造`Phi`并验证`Phi@Phi.T=K`，删除零特征值方向，但明确这不是整个输入空间唯一特征映射；
- 实现正权核矩阵和、逐元素直积以及`diag(g)@K@diag(g)`三种闭包，拒绝负权和样本顺序/形状不一致；
- 以确定性Sigmoid参数构造明显负特征值反例，区分“有限反例足以证伪”和“有限样本通过不足以全局证明”；新增31项测试，与原核SVM共55项，不据此记录个人掌握。

Phase 12 第三十二批已完成并纳入本批提交：

- 正文细项复核确认第2.2.4节要求有限范围/步长调参、算法和参数配置选定后用全部开发数据重训最终模型，并严格区分验证数据与实际测试数据；
- 按参数名稳定排序生成有限笛卡尔积，验证三个参数各五个候选会产生125种配置，并拒绝空、重复、非有限候选；
- 候选阶段每次只在训练集拟合、只在验证集打分，显式区分MSE等越小越好与accuracy等越大越好，同分保留先出现者；
- 选择完成后合并训练集与验证集重新拟合最终模型，测试集不参与选参、预处理或重训；
- 完整入口只对最终模型计算一次测试预测和度量，同时保留选定参数与全部验证记录；
- 记录型测试逐次核对候选拟合、最终重训和度量调用所见样本；新增20项测试，与原交叉验证共27项，不据此记录个人掌握。

Phase 12 第三十三批已完成并纳入本批提交：

- 正文细项复核确认第6.4节在0/1损失后并列给出hinge、指数与对率三种替代损失，原专题只实现hinge；
- 统一以一维带符号间隔计算严格负间隔0/1、hinge、指数和log2缩放对率损失，避免标签列向量广播；
- 对率损失用`logaddexp`稳定计算，梯度按间隔正负分支计算；指数损失对极大负间隔溢出给出明确错误；
- 实现三种替代损失的间隔梯度、Jensen凸性差值和`0.5||w||^2+C*sum(loss)`目标分解；
- 明确hinge在`margin=1`处采用右侧次梯度0，并验证间隔外梯度严格为0；指数/对率在任意有限间隔只会变小而非精确归零；
- 新增29项测试，与原软间隔共53项，不据此记录个人掌握。

Phase 12 第三十四批已完成并纳入本批提交：

- 新建120分钟/150分模拟卷2，题型为严格交易CSV分组汇总、带权无向图Dijkstra、缺失处理/标准化/PCA/kNN完整流水线，与模拟卷1的CSV统计/BFS/逻辑回归不复用表面解法；
- 第1题覆盖唯一ID、非负有限金额、分组count/total/mean、总额降序和类别升序平局；
- 第2题覆盖严格图解析、陈旧堆条目、完整路径恢复、等距时完整结点序列字典序、不可达和起终点相同；
- 第3题使用非连续标签与空白缺失，候选`k=1,3,5`只在训练/验证上选择；PCA用`X.T@X/n`和`eigh`取二维方向并固定符号；
- kNN按平方欧氏距离，邻居等距按训练行顺序，票数平局依次比较类内邻居距离和、数值标签；
- 选参后合并训练/验证从头拟合预处理、PCA和最终邻居库，保存六个`.npz`键并安全恢复，测试集不参与任何拟合；
- 配套题面、计时说明、评分细则、复盘模板、学生空白入口、参考实现、正式数据、逐字节期望结果和自动评分器；参考提交实测150/150；新增20项验收测试，不记录学习者虚假成绩。

Phase 12 第三十五批已完成并纳入本批提交：

- 正文细项复核确认第8.2节用加性模型逐轮最小化指数风险推导AdaBoost，并讨论基学习器不支持样本权重时的重采样；原专题主要覆盖操作步骤；
- 实现均匀/指定初始分布下的指数风险，以及条件正类概率对应的半对数几率最优加性得分；
- 将一轮风险写成`exp(-alpha)*(1-error)+exp(alpha)*error`，用解析式和中心差分验证最优`alpha`及其随弱学习器质量变化的符号；
- 返回分布更新的归一化因子`Z_t`，逐轮保存累计得分、分布、风险和`Z_t`乘积，验证累计指数风险恒等于归一化因子连乘；
- 从初始分布和累计得分直接重建当前样本分布，验证与逐轮更新相同，并检查训练错误不超过指数风险；
- 提供固定随机种子的加权有放回重采样，明确它是重赋权的有限随机近似；新增25项测试，与原AdaBoost共45项，不据此记录个人掌握。

仓库尚未完成。当前优先完成教材第1～10章、配套数据处理、算法题、综合任务和模拟考试；第11～16章已按用户要求暂缓。

## 当前学习检查点

- 已确认能判断 `axis=0/1` 的基本聚合方向。
- 曾混淆 `X[:, 1]` 与 `X[:, 1:2]`，已完成第一次广播迁移解释。
- 尚未据此标记为稳定掌握；后续需要在损失函数或梯度计算中再次验证。
- `02_numpy_basics/01_arrays_shapes_axes/starter.py` 尚未由学习者填写。
- 用户当前选择先继续建设下一批仓库内容，再进入集中练习。

个人学习证据与详细错误以 `progress.md` 和 `bug_book/` 为准。

## 下一建设批次

按以下顺序推进，保持每一项都可以独立验证和提交：

1. 继续复核前10章正文层级的代表方法遗漏；
2. 为前10章增加更多文件读取、模型保存恢复和严格输出的无OJ综合任务；
3. 继续复核前10章正文细项，并增加不复用两套模拟卷表面形式的无OJ综合任务；
4. 当前教材总优先级固定为第1～10章，第11～16章暂缓；
5. 并行保留文件读写、pandas和算法题学生练习入口。

## 当前阻塞与待确认

- 当前建设映射以已核验的2016年第一版目录和正文为准；学习者手中具体印次尚未确认，因此仍不把覆盖矩阵标记为最终教材验收。
- 机试允许的第三方库范围尚未有正式原文确认；核心答案继续保持标准库与 NumPy 可运行。

这些事项不阻塞 NumPy、pandas、文件读写、数据处理和线性模型基础建设。

## 最近验证基线

- 日期：2026-07-17
- 当前专用测试：第8章AdaBoost算法与指数风险视角两组共45项通过
- 全量测试：`python -m pytest -q`
- 结果：2005项通过（41.03秒）
- 依赖检查：`python -m pip check`，未发现冲突
- 数值闭环：条件指数风险、半对数几率最优得分、单轮alpha有限差分、分布归一化、累计Z乘积、得分重建分布、训练错误上界与固定种子重采样均已验证
- Git 状态要求：本批推送后本地 `main` 应与 `origin/main` 一致；恢复时重新运行 `git status`，不要把本行当作实时状态

每次验证后必须用实际结果替换本节，不能仅写“预计通过”。

## 每批结束前检查

- [ ] 检查 `git status`，确认没有覆盖用户未提交的修改；
- [ ] 运行相关局部测试；公共配置变化时运行全量测试；
- [ ] 检查数值、形状、边界输入和输出文件，而不只检查能否运行；
- [ ] 更新本文件的建设状态、下一步、阻塞项和验证基线；
- [ ] 有学习证据时更新 `progress.md`，有代表性错误时更新 `bug_book/`；
- [ ] 教材状态改变时更新 `coverage_matrix.md`；
- [ ] 提交前运行 `git diff --check` 并复核待提交文件；
- [ ] 推送后确认本地分支与 `origin/main` 一致。
