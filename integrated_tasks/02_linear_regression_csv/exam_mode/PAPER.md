# 线性回归 CSV 综合题（建议 75 分钟）

训练和验证文件含 `sample_id,feature_1,feature_2,target`，测试文件不含 target。只用训练集拟合线性模型，在验证集计算 MSE 和 R2，保存模型并重新加载，预测测试集，生成要求格式的预测和指标文件。程序必须支持命令行指定三份输入与三个输出路径，并提供可直接运行的入口。

输出格式见 `expected_format/`；数值保留6位小数。评分：读取与数据处理20分，训练20分，验证15分，保存恢复15分，输出格式20分，完整程序10分。
