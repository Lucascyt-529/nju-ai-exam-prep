# 堆与 Top K：高频元素

输入 `n k` 和 n 个整数，输出频率最高的 k 个不同整数。按频率降序；频率相同时数值升序。

```text
输入:
5 2
1 1 2 2 3
输出: 1 2
```

先用哈希表计数，再用堆选择 k 个优先级最小的 `(-frequency, value)`。当前清晰实现时间约 `O(n + m log k)`，m 为不同元素数，空间 `O(m)`。

边界：k 为0、只有一种值、频率并列、负数。变式：改成求第 k 大元素并说明是否去重。最终从空文件写完整程序，并在输出前落实平局规则。
