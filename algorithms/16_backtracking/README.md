# 回溯：生成所有子集

输入 n 和 n 个互不相同的整数；按“先不选、再选择当前元素”的递归顺序输出全部子集，空集打印 `EMPTY`。

```text
输入:
2
1 2
输出:
EMPTY
2
1
1 2
```

每个位置有选与不选两条分支。进入选择分支前 `append`，返回后必须 `pop` 恢复现场；保存答案时要复制 path。时间 `O(n*2^n)`（含复制输出），递归空间 `O(n)`。

边界：n=0 仍有一个空集；输入有重复值时需另定去重规则。变式：生成和等于 target 的组合并剪枝。最终从空文件完成读取、递归、输出和 `main()`。
