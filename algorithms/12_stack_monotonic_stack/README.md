# 单调栈：每日温度

输入 n 和 n 个温度，对每一天输出还需等待几天才出现严格更高温度；若不存在则为0。

```text
输入:
4
30 40 35 50
输出: 1 2 1 0
```

栈保存尚未找到答案的下标，并让对应温度单调不增。新温度更高时持续弹栈并填写距离。每个下标至多入栈、出栈一次，时间 `O(n)`、空间 `O(n)`。

边界：空输入、严格下降、相等温度（不算更高）。变式：改为输出下一个更大值而不是距离。完成后从空文件组织输入、算法、输出与 `main()`。
