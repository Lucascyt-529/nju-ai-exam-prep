# 贪心与区间：合并闭区间

输入 n 个闭区间 `[left,right]`，输出合并后的不重叠区间，按左端升序。端点相接视为重叠。

```text
输入:
3
1 3
2 6
8 9
输出:
1 6
8 9
```

先按左端排序。扫描时，若新区间左端超过当前合并区间右端就开新区间，否则只扩展右端。排序保证未来区间不可能再改变已结束区间，这就是贪心成立的原因。时间 `O(n log n)`、额外结果空间 `O(n)`。

边界：空输入、单区间、包含关系、端点相接。变式：改为选择最多个互不重叠区间，此时贪心关键字变为最早结束时间。最终从空文件完成解析、排序、合并和输出。
