# 二叉树：层序遍历

输入一行层序编码，`#` 表示空节点；输出各层，层间用 `|` 分隔。

```text
输入: 3 9 20 # # 15 7
输出: 3 | 9 20 | 15 7
```

构造时队列保存待补左右孩子的节点；遍历时每轮固定读取当前队列长度，从而得到一整层。时间 `O(n)`、队列空间 `O(w)`，w 为最大层宽。

边界：`#` 空树、单节点、只有一侧孩子。变式：给前序和中序序列重建无重复值二叉树。必须说明输入编码，否则树结构不唯一，并从空文件写出构造、遍历、输出和 `main()`。
