# 前缀和：和为目标值的子数组

第一行输入 `n target`，第二行输入 n 个整数；输出和恰为 target 的连续子数组数量。

```text
输入:
3 2
1 1 1
输出: 2
```

若当前位置前缀和是 `s`，左侧出现过 `s-target`，两者之差就是 target。哈希表记录各前缀和出现次数，初始放入 `{0:1}`。时间 `O(n)`、空间 `O(n)`。

边界包括空序列、负数、target 为0；滑动窗口不能直接处理含负数的此题。变式：输入多组 target，比较逐次扫描与预处理限制。最终需从空文件写出解析、算法、输出和 `main()`。
