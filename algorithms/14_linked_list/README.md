# 链表：反转单链表

输入节点数 n 和 n 个整数，构造单链表、原地反转指针，再按新顺序输出节点值。

```text
输入:
3
1 2 3
输出: 3 2 1
```

循环维护 `previous/current/following`：先保存后继，再反转当前指针，最后推进。时间 `O(n)`、除节点外额外空间 `O(1)`。构造和输出函数让本题成为可直接运行的完整程序。

边界：空链表、单节点、两节点。变式：增加 Floyd 快慢指针判断是否有环；有环时禁止直接遍历输出。完成后从空文件重建节点类、输入、算法和 `main()`。
