# 当前学习状态

## 正在学习

- 线性回归

## 已独立完成

- NumPy 数组形状、`axis`、索引与筛选的首轮练习
- `reshape`、转置与拼接
- 广播与矩阵乘法
- 训练集均值填补、标准化及参数复用
- 训练集、验证集、测试集职责与回归指标
- 线性回归的 `predict`、MSE、解析梯度与批量梯度下降

以上是已有现场学习证据，不代表已经长期熟练；仓库中存在参考实现的其他算法不计入个人掌握。

## 仍需复验

- `(n,)` 与 `(n, 1)` 的区别
- `X.T @ error` 的形状
- 训练集参数在验证集和测试集上的复用
- 训练损失下降与验证性能改善的区别

## 当前任务

- 完成线性回归 `starter.py`，重点补全 `fit_least_squares`
- 运行 `demo.py` 与 `check.py`
- 从空文件重写一次
- 完成一个双特征或带噪声变式
- 在线性回归 CSV 综合任务中复验读取、训练、保存与输出

## 下一步

- 逻辑回归
