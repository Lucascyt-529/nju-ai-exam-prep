# 新对话从这里继续

本文件是当前学习入口，不再是第一次 NumPy 诊断入口。基础阶段已经收尾，下一次对话直接转入机器学习主体。

## 新对话启动指令

把下面这段话发给 Codex：

> 请依次阅读 `AGENTS.md`、`PROJECT_STATE.md`、`progress.md`、`START_HERE.md`，并检查 `git status` 与最近提交。不要重新建设仓库，也不要从 NumPy 第一课重讲。根据已记录的真实学习证据，从模型评估的最小知识激活开始，然后进入线性回归 NumPy 手写。继续遵循“先预测、再小步实现、集中审查、变式迁移、更新进度”的教学方式；不要把参考实现当成我的掌握证据。

## 已完成的个人学习基础

- 数组形状、`axis`、一维数组与列向量；
- reshape、转置、按轴拼接；
- 花式索引、布尔筛选、`X/y` 同步打乱；
- 按特征与按样本广播、数组复制和零标准差处理；
- 矩阵乘法、线性输出、多输出矩阵和 `X.T @ error`；
- 训练集均值填补、标准化以及测试集复用训练参数。

上述内容属于首轮通过，仍需在线性模型和完整数据任务中复验，不能默认已经长期熟练。具体错误见 `bug_book/001_*.md` 至 `bug_book/005_*.md`。

## 下一阶段路线

```text
模型评估最小激活
→ 留出法、训练/验证/测试职责
→ 回归指标 MSE、RMSE、R²
→ 线性回归预测与损失
→ 解析梯度与梯度下降
→ CSV读取、训练集预处理、模型训练、验证和结果输出
→ 变式迁移与错题复盘
```

模型评估只讲支撑后续代码所需的最少理论，不在一开始陷入全部统计检验。线性回归进入代码时，先使用：

```text
watermelon_book/02_model_evaluation_selection/
watermelon_book/03_linear_models/
```

Codex 应先选择其中最基础的小专题，不让学习者直接浏览参考答案，也不一次布置过多函数。

## 教学边界

- 核心算法默认只使用 Python 标准库和 NumPy，不使用 sklearn 代写；
- 正常输入下的算法、形状、数值稳定和数据泄漏优先，重复的工程防御校验暂缓；
- 每次审查一次列完本轮所有问题，让学习者集中修改；
- 每完成一个专题更新 `progress.md` 和 `PROJECT_STATE.md`，代表性错误写入 `bug_book/`；
- 小批次验证后可按既有授权直接提交并推送 `main`。

## 当前验证基线

- 全仓：`2101 passed`；
- 最近完成提交：运行 `git log -1 --oneline` 获取，不在本文硬编码提交哈希；
- 新对话必须先确认工作树是否干净，文件记录与代码不一致时以实际代码和测试为准。
